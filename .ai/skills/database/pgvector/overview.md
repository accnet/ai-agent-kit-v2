# pgvector Overview

Use this skill for PostgreSQL `pgvector` schema/index/query tuning. Confirm PostgreSQL and extension versions before changes.

Inspect existing table layout, ANN index type, and migration strategy first.
