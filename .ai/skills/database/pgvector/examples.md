# pgvector Examples

- Add table columns: `embedding vector(n)`, `embedding_model text`, `source_id`.
- Create index with measured ivfflat/hnsw settings.
- Evidence: explain analyze + recall@k benchmark.
