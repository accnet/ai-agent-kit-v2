# Embeddings Patterns

- Maintain embedding model/version metadata with each vector.
- Normalize text consistently across indexing and query pipelines.
- Batch embedding jobs with retry and dead-letter handling.
- Re-embed incrementally when source docs change.
