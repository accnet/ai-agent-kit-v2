# Embeddings Examples

- Add `embedding_model` and `embedding_version` fields to vector records.
- Write a migration plan for model upgrade with dual-write or blue/green index.
- Evidence: dimension check tests and reindex report.
