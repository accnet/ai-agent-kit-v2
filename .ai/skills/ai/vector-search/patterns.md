# Vector Search Patterns

- Apply metadata filters (tenant/ACL/type) before ranking.
- Tune `k`, distance metric, and ANN settings with benchmark evidence.
- Support hybrid retrieval (vector + lexical) for precision-sensitive tasks.
- Return score + provenance fields for downstream grounding.
