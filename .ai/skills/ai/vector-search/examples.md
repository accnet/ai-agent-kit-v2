# Vector Search Examples

- Add query API returning `{results:[{id,score,source}], filters_applied}`.
- Add benchmark case comparing two ANN parameter sets.
- Evidence: recall@k/latency report and filter correctness tests.
