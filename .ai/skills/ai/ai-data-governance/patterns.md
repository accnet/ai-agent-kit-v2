# AI Data Governance Patterns

- Classify data by sensitivity before ingestion.
- Enforce least-privilege access to training/evaluation/production datasets.
- Keep lineage from source record -> transformed chunk -> model interaction.
- Implement retention and deletion workflows for prompts, vectors, and traces.
