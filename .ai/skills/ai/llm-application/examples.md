# LLM Application Examples

- Introduce a typed orchestration function returning `{result, diagnostics}`.
- Add output validator + retry-on-parse-failure with bounded attempts.
- Attach evidence: unit tests for each failure class and one end-to-end flow.
