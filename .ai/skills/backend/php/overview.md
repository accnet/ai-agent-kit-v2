# PHP Overview

Use this skill for PHP service or library changes. First identify runtime and framework versions (PHP, Composer lock, framework adapters) before applying patterns.

Keep changes aligned with existing project conventions for dependency injection, HTTP boundaries, and error mapping.
