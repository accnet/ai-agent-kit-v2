# PHP Backend

Use when a task modifies PHP application code, Composer packages, configuration, or server-side request handling. Inspect the project's PHP version (`php --version`), framework (Laravel, Symfony, Slim, or plain PHP), and Composer lock file before making changes.
