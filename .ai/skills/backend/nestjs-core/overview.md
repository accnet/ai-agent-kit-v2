# NestJS Core

Use for NestJS modules, controllers, providers, DTO validation, guards, interceptors,
exception filters, and configuration. NestJS's dependency injection and decorator-based
structure means most correctness bugs come from a misplaced concern (business logic in a
controller, a repository injected past the service layer) rather than from the framework
itself — check the module boundary before writing new code.
