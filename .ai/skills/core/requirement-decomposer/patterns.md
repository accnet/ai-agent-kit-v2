# requirement-decomposer Patterns

One task per verifiable acceptance criterion; order with `--needs`, track contract drift with `--depends-on`, tag `--context`/`--epic` only against already-registered names.
