# requirement-decomposer Best Practices

Use the checklist in SKILL.md, confirm `ai-kit ready` returns a runnable task before handoff, and re-plan via `update-task` rather than silently extending scope.
