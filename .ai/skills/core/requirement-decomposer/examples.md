# requirement-decomposer Evidence

Attach the source brief's acceptance criteria alongside the resulting task list, and the `ai-kit ready`/`ai-kit graph` output confirming the dependency graph resolves.
