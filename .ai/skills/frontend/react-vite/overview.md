# React Vite

Use for React applications built with Vite: component boundaries, routing, data loading,
env config, and build/test integration. Vite's dev server is not the production build —
`vite build` + `vite preview` is the only way to exercise the real bundling, chunking, and
`import.meta.env` inlining path.
