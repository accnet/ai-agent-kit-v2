# Vite Frontend

Use for Vite configuration, build modes, environment variables, aliases, asset handling,
code splitting, and production build checks — this is about the bundler/build layer
itself, independent of which UI framework (React, Vue, Nuxt) sits on top of it.
