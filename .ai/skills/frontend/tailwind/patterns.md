# Tailwind Patterns

- Build from design tokens and reusable component class maps.
- Use responsive/state variants intentionally (`md:`, `hover:`, `focus:`).
- Keep conditional class composition centralized.
- Pair visual changes with accessibility checks (contrast/focus states).
