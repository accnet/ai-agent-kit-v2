# TypeScript Patterns

- Encode API/data contracts with explicit interfaces/types.
- Prefer discriminated unions for state machines and error variants.
- Use utility types judiciously; preserve readability.
- Keep runtime validators at trust boundaries.
