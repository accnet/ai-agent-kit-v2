# TypeScript Best Practices

- Avoid `any`; use `unknown` + narrowing when needed.
- Keep public types stable and exported from clear module boundaries.
- Use exhaustive checks in switch statements.
- Align tests with changed type-level guarantees.
