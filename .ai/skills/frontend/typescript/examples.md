# TypeScript Examples

- Add discriminated union for operation result (`ok`/`error`).
- Introduce parser function that validates unknown input to typed DTO.
- Evidence: typecheck output and targeted behavior tests.
