# Next.js Patterns

- Keep server components for data-heavy rendering; isolate client components for interactivity.
- Use route handlers/API routes for privileged logic.
- Cache and revalidation settings should match data freshness requirements.
- Prefer shared typed fetch helpers for backend calls.
