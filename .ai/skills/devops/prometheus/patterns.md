# Prometheus Patterns

- Expose low-cardinality metrics with stable label sets.
- Define recording rules for expensive repeated queries.
- Alert on symptoms (SLO burn/error rate) with actionable annotations.
- Keep scrape configs explicit and environment-scoped.
