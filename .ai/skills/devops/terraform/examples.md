# Terraform Examples

- Add module variable and output with backward-compatible defaults.
- Run `terraform plan` and attach summary of expected changes.
- Evidence: plan output hash and post-apply validation checks.
