# Terraform Patterns

- Encapsulate reusable infra in modules with explicit inputs/outputs.
- Keep environment overlays thin; avoid copy-pasted stacks.
- Use data sources sparingly and document external dependencies.
- Validate plans in CI and require human approval for applies.
