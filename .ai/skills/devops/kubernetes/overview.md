# Kubernetes

Use when a task modifies Kubernetes manifests, Helm charts, Kustomize overlays, resource quotas, RBAC policies, or cluster configuration. Inspect the project's Kubernetes version (`kubectl version`), manifest management tool, and existing namespace layout before making changes.
