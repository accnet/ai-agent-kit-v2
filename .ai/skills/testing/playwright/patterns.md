# Playwright Patterns

- Prefer stable locators (role/test-id) over brittle CSS chains.
- Model user journeys with setup helpers and deterministic test data.
- Capture network stubs only when backend dependency must be isolated.
- Use tracing/screenshots/video selectively for failing tests.
