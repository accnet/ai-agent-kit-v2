# Pytest Patterns

- Use fixtures for setup/teardown and dependency injection.
- Parametrize behavior matrices instead of copy-pasting cases.
- Keep unit tests isolated; use integration markers for external dependencies.
- Assert observable behavior, not private implementation details.
